{% block pagination %}
    <nav aria-label="Page navigation example">
        <ul class="pagination">
            {% for page in paginator  %}
                {{ page | raw }}
            {% endfor %}
        </ul>
    </nav>
{% endblock %}
